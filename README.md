Project: Chapter1_Challenge_1_2
Overview

This program analyzes a list of lottery numbers stored as strings. It removes dashes, converts each digit into integers, calculates the sum and average of the digits, and determines which lottery number has the highest average.

Features

Uses arrays to store lottery numbers

Uses string manipulation to remove dashes

Converts characters to integers

Uses both for and for-each loops

Calculates digit sum and average
